[connection.refreshTokens.log.debug.refreshingTokens]
one = 'Refreshing access tokens'

[connection.refreshTokens.log.debug.tokensRefreshed]
one = 'Access tokens refreshed'

[connection.tokenNeedsRefresh.log.debug.expiryCheckError]
one = 'Error while checking token expiry: {{.Reason}}'

[connection.tokenNeedsRefresh.log.debug.tokenIsStillValid]
one = 'Token is still valid. Expires in {{.TimeLeft}}'

[connection.error.missingBootstrapURL]
one = 'bootstrap URL is missing for Kafka instance "{{.Name}}"'