package flags

const (
	// FlagID is a flag representing a Kafka ID
	FlagID = "id"
	// FlagName is a flag representing a Kafka name
	FlagName = "name"
	// FlagOwner is a flag representing a Kafka owner name
	FlagOwner = "owner"
	// FlagProvider is a flag representing an OCM provider ID
	FlagProvider = "provider"
	// FlagRegion is a flag representing an OCM region ID
	FlagRegion = "region"
	// FlagMultiAZ is a flag representing a whether a Kafka is multi AZ
	FlagMultiAZ = "multi-az"
	// FlagClusterID is a flag representing a Kafka's cluster ID
	FlagClusterID = "cluster-id"
)
