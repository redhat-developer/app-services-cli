.DS_Store
dist
/rhoas
node_modules
**/package-lock.json
.idea

# These are ignored because they are the common extension
# formats for credentials which can get included during development
# You can explicitly override these on a per file basis with a "!" prefix
# Example: /!keycloak.json
/*.json
!renovate.json
/*.yaml
/*.yml
/*.env
/*.properties
.idea/
.vscode/settings.json

.notes
