package generate

import (
	"github.com/MakeNowJust/heredoc"
)

var (
	templateEnv = heredoc.Doc(`
		## Generated by rhoas context generate-config
		{{if .KafkaURL}}KAFKA_URL={{.KafkaURL}} 
		{{end}}{{if .RegistryURL}}REGISTRY_URL={{.RegistryURL}}
		{{end}}CLIENT_ID={{.ClientID}}
		CLIENT_SECRET={{.ClientSecret}}
`)

	templateJSON = heredoc.Doc(`
		{	
			{{if .KafkaURL}}"kafkaUrl":"{{.KafkaURL}}",
			{{end}}{{if .RegistryURL}}"registryUrl":"{{.RegistryURL}}",
			{{end}}"clientID":"{{.ClientID}}",
			"clientSecret":"{{.ClientSecret}}"
		}
	`)

	templateProperties = heredoc.Doc(`
		## Generated by rhoas context generate-config
		{{if .KafkaURL}}kafkaUrl={{.KafkaURL}}
		{{end}}{{if .RegistryURL}}registryUrl={{.RegistryURL}}
		{{end}}clientID={{.ClientID}}
		clientSecret={{.ClientSecret}}
	`)
)
